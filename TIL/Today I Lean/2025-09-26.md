## TIL 2025-09-25

### 오늘 배운 것

#### 웹페이지기반 응답하기

```python
import bs4 # 웹페이지 파싱용 라이브러리`
from langchain_community.document_loaders import WebBaseLoader

# 와사칸 물어보기
print(llm_4o_mini.invoke("너 멕시카나치킨의 ceo인 와삭칸이라고 알아? 그 사람에 대해 알려줘").content)

# 웹페이지를 읽어주는 로더 생성
web_loader = WebBaseLoader(
    web_path = [URL], # 읽어들일 웹페이지 등록 (여러개 가능)
    bs_kwargs = dict( # 파싱도구 bs와 관련된 파라미터 넣기
        parse_only = bs4.SoupStrainer(
            'div', attrs={'id':['article-body']} # 파싱해올 부분 선택
        )
    )
)
```